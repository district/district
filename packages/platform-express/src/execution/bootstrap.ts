export function bootstrap(module: any) {
    // first resolve child injectors - built in to core
    // extract all resolved routes into their own array
    // extract middlewares from their array and resolve them into express
    // cycle over routes to get methods
    // register routes with Express
}